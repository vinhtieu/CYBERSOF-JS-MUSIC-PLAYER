export default class Track {
  constructor(image, title, singer, song, duration) {
    (this.image = image),
      (this.title = title),
      (this.singer = singer),
      (this.song = song),
      (this.duration = duration);
  }
}
